[Unit]
Description=Run script when CS:GO opens or closes

[Service]
Type=forking
PIDFile=/tmp/csgo_handler.pid
ExecStart=/usr/bin/csgo_handler -D
ExecStop=/bin/kill -s QUIT $MAINPID

[Install]
WantedBy=default.target
